import { IgnoreHtmlPipe } from './ignore-html.pipe';

describe('IgnoreHtmlPipe', () => {
  it('create an instance', () => {
    const pipe = new IgnoreHtmlPipe();
    expect(pipe).toBeTruthy();
  });
});
